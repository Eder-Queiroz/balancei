import 'package:flutter/material.dart';

class CommonRadius {
  static const small = Radius.circular(4.0);
  static const medium = Radius.circular(8.0);
  static const large = Radius.circular(12.0);
  static const extraLarge = Radius.circular(16.0);
}
